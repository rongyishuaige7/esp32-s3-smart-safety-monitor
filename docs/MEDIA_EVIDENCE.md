# 项目素材说明

> 整理日期：2026-07-18

## 文件来源与处理

- 本地 Hardware Lab 素材中心保留原始文件；本仓库收录适合公开展示的处理后副本。
- 图片已按需要移除元数据、遮盖私有信息或缩放；具体处理方式见下表。

## 已公开文件

| 公开路径 | 拍摄日期 / 来源日期 | 脱敏处理 | 文件说明 |
|:--|:--|:--|:--|
| `hardware/eda/schematic.pdf` | 2026-07-18 提供的导出文件 | 从副本移除 PDF 元数据 | EDA 导出。原理图显示 GPIO9 上只有一个 SG90，而当前源码定义 GPIO9 和 GPIO10 两个舵机；应将该 EDA 视为不完整，不能作为当前接线权威。 |
| `hardware/eda/pcb-preview.pdf` | 2026-07-18 提供的导出文件 | 从副本移除 PDF 元数据 | EDA 导出。原理图显示 GPIO9 上只有一个 SG90，而当前源码定义 GPIO9 和 GPIO10 两个舵机；应将该 EDA 视为不完整，不能作为当前接线权威。 |
| `hardware/eda/bom-export.csv` | 2026-07-18 提供的导出文件 | 从 UTF-16LE 表格导出转换为 UTF-8 CSV；已省略供应商、店铺和价格字段 | EDA 导出。原理图显示 GPIO9 上只有一个 SG90，而当前源码定义 GPIO9 和 GPIO10 两个舵机；应将该 EDA 视为不完整，不能作为当前接线权威。 |
| `hardware/eda/gerber/` | 2026-07-18 提供的导出文件 | 从副本解包 Gerber/钻孔成员；已省略第三方下单说明文本 | EDA 导出。原理图显示 GPIO9 上只有一个 SG90，而当前源码定义 GPIO9 和 GPIO10 两个舵机；应将该 EDA 视为不完整，不能作为当前接线权威。 |

## 未公开材料

未列于上表的文件没有复制到本仓库。常见原因包括重复或错投文件、私人标识、过时界面语义或接线信息不完整。原始文件仍保留在本地素材中心。
